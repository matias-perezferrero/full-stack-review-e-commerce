import React from 'react'

export default function shopHome() {
    return (
        <div style={{width: '100%', height: "80vh", display: "flex", justifyContent: 'center', alignItems: 'center'}}>
            Choose a category to start spending money now!
        </div>
    )
}
